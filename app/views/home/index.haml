%h1 Challenge of the Week
%h2= flash[:notice]

= form_tag('/challenge', :method => 'put') do
  = label_tag(:content, 'Content')
  = text_field_tag(:content, @challenge)
  = submit_tag('Edit')

%h3 Current Entries
- @entries.each do |entry|
  %p= entry
